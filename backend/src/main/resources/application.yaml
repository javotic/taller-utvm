spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/mi_negocio_db
    username: admin_db
    password: password_seguro
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true